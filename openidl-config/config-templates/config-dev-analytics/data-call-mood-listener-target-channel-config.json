{
    "targetChannels": [{
        "channelName": "defaultchannel",
        "chaincodeName": "openidl-cc-default"
    }],
    "users": [{
        "org": "${ANALYTICS_ORGNAME}",
        "user": "openidl-${ANALYTICS_ORGNAME}-data-call-mood-listener-ibp-2.0",
        "mspId": "${ANALYTICS_ORGNAME}MSP"
    }]
}