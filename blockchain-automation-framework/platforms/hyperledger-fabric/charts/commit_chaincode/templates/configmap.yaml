{{ if .Values.chaincode.usecollectionconfig }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: commit-chaincode-{{ $.Values.channel.name }}-collection-config
  namespace: {{ $.Values.metadata.namespace }}
  labels:
    app.kubernetes.io/name: commit-chaincode-{{ $.Values.channel.name }}-collection-config
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    {{- include "labels.custom" . | nindent 2 }}
data:
  collection.config.base64: {{ .Values.chaincode.collectionconfig | quote }}
{{ end }}

